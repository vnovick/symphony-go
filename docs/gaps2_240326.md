# Gap Analysis: symphony-go (2026-03-24, rev 15)

Full-codebase review after seven rounds of fixes. Rev 11 adds a deep parallel pass from a senior Go architect, senior frontend architect, and a TUI-vs-Web wiring analyst covering race conditions, goroutine leaks, dead code, DRY violations, silent failures, and feature-parity gaps. Covers **Go backend**, **TypeScript/React frontend**, and **TUI vs Web wiring** perspectives.

Rev 7 fixes: FE-GA-M3, FE-GA-M1, FE-GA-M6, FE-GA-H5, FE-GA-L2, FE-GA-L7, FE-GA-L8, GO-NEW-24, GO-NEW-26, FE-NEW-24, GO-NEW-27.

Rev 8 fixes: GO-R8-1, GO-R8-4, GO-R8-6, FE-R8-005, FE-R8-007, FE-R8-017.

Rev 9 fixes: FE-R9-8, FE-R9-9, FE-R9-14, FE-R9-4, GO-R9-2, GO-R9-L1.

Rev 10 fixes: GO-R5-3, GO-NEW-21, GO-R10-1, GO-R10-2, GO-R10-3, GO-R10-4, GO-R10-5, FE-R10-1, FE-R10-2, FE-R10-3, FE-R10-4, FE-R10-5, FE-R10-6, FE-R10-7.

Rev 11 fixes: GO-R10-6, FE-R11-1, GO-R11-8. Rev 11 verification: GO-R11-2 (already fixed), FE-R11-18 (already fixed); GO-R11-19 line count corrected to ~885.

Rev 11 follow-up fixes: FE-R11-7 (viewport useMemo→useEffect), FE-R11-9 (AgentLogPanel Zustand selector narrowing), GO-R11-7 (removed unused cfg param from AvailableSlots), GO-R11-14 (added race-free comment to ReconcileStalls).

Rev 12 fixes (Go Backend): GO-R11-9, GO-R11-11, GO-R11-12, GO-R11-15, GO-R11-16, GO-R11-22, GO-R11-23, GO-R11-27. Rev 12 verification: GO-R11-1, GO-R11-3, GO-R11-4, GO-R11-5, GO-R11-6, GO-R11-10, GO-R11-13, GO-R11-17, GO-R11-18, GO-R11-19, GO-R11-25 confirmed fixed in prior revisions — false positives.

Rev 13 fixes (Frontend only): FE-R11-11, FE-R11-13, FE-R11-16, FE-R11-19, FE-R11-23, FE-R11-27, FE-R11-29, FE-R11-30.

Rev 14 fixes: GO-R11-28 (stall now sends populated RunEntry with TerminalStalled; reviewer routes history through EventReviewerCompleted), GO-R11-29 (orchestrator split confirmed complete), WIRE-1/11 (dead route removed), WIRE-3/10 (clear-logs UI wired to Logs page), WIRE-6 (backlog dispatch button + prop chain), WIRE-12 (TUI cancel now posts tracker comment), WIRE-14 (useIssue fresh-fetch on modal open), WIRE-15 (AI Review button added to RunningSessionsTable). WIRE-8/13 and FE-R11-15 removed — AgentMode feature intentionally dropped.

Rev 15 fixes: GO-R11-21 (OrchestratorClient interface — 19 func fields replaced with typed interface; noopClient, FuncClient adapters; orchestratorAdapter in main.go; all tests migrated to FuncClient), GO-R11-20 (buildSnapFunc and buildTUIConfig helpers extracted from run(); inline blocks replaced with two-line calls), WIRE-2/5 (Linear project filter web UI — ProjectFilterCard in Settings, setProjectFilter in useSettingsActions, PUT /settings/project-filter handler). Rev 15 verification: FE-R11-2, FE-R11-3, FE-R11-4, FE-R11-5, FE-R11-6, FE-R11-8, FE-R11-10, FE-R11-12, FE-R11-21, FE-R11-24, FE-R11-25, FE-R11-32 confirmed fixed in prior revisions — false positives in summary table.

---

## Severity Legend

- 🔴 **High** — Functional bug, data race, broken API contract, or data permanently wrong
- 🟡 **Medium** — Correctness gap, silent failure, or missing validation in real-world scenarios
- 🟢 **Low** — DRY violation, code smell, observability gap, UX polish

---

## Open Items (rev 15)

> **All tracked items have been fixed.** No open items remain.

## Summary Table — Open Items (rev 15)

_No open items._

---

## Closed Items (all sessions)

| ID | Status |
|----|--------|
| GO-R11-20 | ✅ Fixed rev 15 — `buildSnapFunc` and `buildTUIConfig` helpers extracted from `run()`; inline blocks replaced with two-line calls |
| GO-R11-21 | ✅ Fixed rev 15 — `OrchestratorClient` interface replaces 19 raw func fields; `noopClient`, `FuncClient` adapters added; `orchestratorAdapter` wires live orchestrator in `main.go`; all tests migrated to `FuncClient` |
| WIRE-2/5 | ✅ Fixed rev 15 — `ProjectFilterCard` added to Settings (conditional on `trackerKind === 'linear'`); `setProjectFilter` action in `useSettingsActions`; `PUT /settings/project-filter` handler in server |
| FE-R11-2 | ✅ Fixed (prior rev, verified rev 15) — `close` useCallback moved before the useEffect that lists it as a dep |
| FE-R11-3 | ✅ Fixed (prior rev, verified rev 15) — `priorityDotClass` uses `p === null \|\| p === undefined` instead of `!p` |
| FE-R11-4 | ✅ Fixed (prior rev, verified rev 15) — `sortedIssues` wrapped in `useMemo`; dep array uses memoized value |
| FE-R11-5 | ✅ Fixed (prior rev, verified rev 15) — `handleRefresh` catch calls `addToast('Refresh failed — check the server.', 'error')` |
| FE-R11-6 | ✅ Fixed (prior rev, verified rev 15) — `sortedIssues` memoized with `useMemo([issues])` |
| FE-R11-8 | ✅ Fixed (prior rev, verified rev 15) — shared `toastApiError(err)` helper in `queries/issues.ts`; used in all mutation `onError` sites |
| FE-R11-10 | ✅ Fixed (prior rev, verified rev 15) — `ReactQueryDevtools` guarded with `import.meta.env.DEV` |
| FE-R11-12 | ✅ Fixed (prior rev, verified rev 15) — wildcard route renders `<NotFound />` |
| FE-R11-21 | ✅ Fixed (prior rev, verified rev 15) — log query `staleTime` is now conditional (`isLive ? 1000 : 15_000`) |
| FE-R11-24 | ✅ Fixed (prior rev, verified rev 15) — `proseClass` exported from `format.ts`; imported in both components |
| FE-R11-25 | ✅ Fixed (prior rev, verified rev 15) — `EMPTY_PROFILES` exported from `format.ts`; imported in both components |
| FE-R11-32 | ✅ Fixed (prior rev, verified rev 15) — module-level `EMPTY_PROFILES`/`EMPTY_BACKLOG_STATES` constants replace `useMemo` in Dashboard |
| GO-H1 | ✅ Fixed rev 2 — `prCtx == nil` guard on `FindOpenPRURL` |
| GO-H2 | ✅ Fixed rev 3 — `BranchName` propagated via `sendExitWithBranch` |
| GO-H3 | ✅ Fixed rev 2 — nil guard before `*o.runCtx.Load()` deref |
| GO-M1 | ✅ Fixed rev 2 — dead `"DRAFT"` check removed; `isDraft` field added |
| GO-M2 | ✅ Fixed rev 2 — `< maxFullDiffLines` → `<= maxFullDiffLines` |
| GO-M3 | ✅ Fixed rev 2 — `context.WithTimeout(context.Background(), 60s)` for post-run push/comment |
| GO-M4 | ✅ Fixed rev 4 — misleading `parseLogLine` comment corrected |
| GO-M5 | ✅ Fixed rev 4 — `FetchPRContext` godoc documents `reviews` scope |
| GO-M6 | ✅ Fixed rev 4 — `workspace.IsDefaultBranch` unified |
| GO-M7 | ✅ Fixed rev 2 — `LANG=C LC_ALL=C` on git subprocesses |
| GO-M8 | ✅ Fixed rev 2 — `profileName = ""` when profile not found |
| GO-M9 | ✅ Fixed rev 2 — `AssertContained` called before early-return |
| GO-M10 | ✅ Fixed rev 2 — `slog.Debug` on CheckPR error in `Detect` |
| GO-L1 | ✅ Fixed rev 2 — `\d+\b` word boundary in PR URL regex |
| GO-L3 | ✅ Fixed rev 4 — `fakeGH` heredoc replaced with `printf '%s'` |
| GO-L4 | ✅ Fixed rev 2 — `slog.Debug` on git fetch failure |
| GO-L5 | ✅ Fixed rev 4 — `liquidEngine` package-level singleton |
| GO-L6 | ✅ Fixed rev 4 — `removeWorktree`/`RemoveWorkspace` accept `context.Context` |
| GO-L7 | ✅ Fixed rev 4 — `cumulativeCached` tracked in `TotalTokens` |
| GO-L8 | ✅ Fixed rev 2 — dead nil guard in `labelsValue` removed |
| GO-L8-NEW | ✅ Fixed rev 4 — `TerminalTimedOut` removed from comment |
| GO-L9 | ✅ Fixed rev 2 — retry comment clarified |
| GO-L9-NEW | ✅ Fixed rev 4 — `runReviewerWorker` calls `prdetector.Detect` |
| GO-L10 | ✅ Fixed rev 2 — `ghCommentCmd.Dir` removed |
| GO-L2 | ✅ Fixed rev 6 — 4 `FetchPRContext` tests added: blank bodies excluded, empty wsPath, git diff populated, gh failure non-fatal |
| GO-NEW-1 | ✅ Fixed rev 5 — `MaxTurns`/hook fields snapshot under `cfgMu.RLock()` at start of `runWorker` |
| GO-NEW-2 | ✅ Fixed rev 5 — re-read live state inside `userTerminatedMu` write lock |
| GO-NEW-3 | ✅ Fixed rev 5 — 8-slot semaphore on GitHub concurrent fetches |
| GO-NEW-4 | ✅ Fixed rev 5 — unreachable overflow guard removed |
| GO-NEW-5 | ✅ Fixed rev 5 — `IsEligible` collapsed to `return IneligibleReason(...) == ""` |
| GO-NEW-6 | ✅ Fixed rev 6 — `started atomic.Bool` guards `SetHistoryFile`/`SetHistoryKey` post-Run |
| GO-NEW-7 | ✅ Fixed rev 5 — failure-path `runAfterHook` uses `context.Background()` |
| GO-NEW-8 | ✅ Fixed rev 5 — `handleLogs` flushes before discarding on buffer-full |
| GO-NEW-9 | ✅ Fixed rev 5 — `BumpMaxWorkers(delta)` atomic read-modify-write |
| GO-NEW-10 | ✅ Fixed rev 6 — `bufLogEntry` moved to `internal/domain.BufLogEntry`; both packages use type alias |
| GO-NEW-11 | ✅ Fixed rev 6 — `Load()` godoc clarified: ValidateDispatch required before Orchestrator.Run |
| GO-NEW-12 | ✅ Fixed rev 5 — `ValidateDispatch` rejects SSH hosts starting with `-` or containing whitespace |
| GO-NEW-13 | ✅ Fixed rev 5 — eliminated by GO-NEW-5 |
| GO-NEW-20 | ✅ Fixed rev 6 — `FetchPRContext` logs debug message on JSON unmarshal failure |
| GO-R5-2 | ✅ Fixed rev 5 — mid-turn `TotalTokens` includes `cumulativeCached` |
| GO-R5-4 | ✅ Fixed rev 6 — `handleSetAutoClearWorkspace` uses `*bool`; absent field → HTTP 400 |
| GO-R5-5 | ✅ Fixed rev 6 — `handleUpdateTrackerStates` rejects empty `activeStates` |
| GO-R5-6 | ✅ Fixed rev 5 — `cfgMu.RLock` around `BacklogStates`/`ActiveStates` in `EventTerminatePaused` |
| FE-H1 | ✅ Fixed rev 2 — poll path uses `StateSnapshotSchema.parse()` |
| FE-H2 | ✅ Fixed rev 2 — `pollIntervalMs` added to `StateSnapshotSchema` |
| FE-H3 | ✅ Fixed rev 3 — `useRef` timer cleanup on unmount in `IssueDetailModal` |
| FE-H4 | ✅ Fixed rev 3 — unsafe `a[sortKey]` replaced with `getVal` helper |
| FE-M1 | ✅ Fixed rev 4 — Cancel Retry button in retrying state |
| FE-M2 | ✅ Fixed rev 4 — `invalidateQueries(ISSUES_KEY)` on modal open |
| FE-M3 | ✅ Fixed rev 5 — `useCancelIssue` uses `patchSnapshot` |
| FE-M4 | ✅ Fixed rev 5 — `patchSnapshot` no-op when `snapshot === null` |
| FE-M5 | ✅ Fixed rev 5 — `useResumeIssue` full `onMutate` with rollback |
| FE-M6 | ✅ Fixed rev 4 — `isInReview` uses exact `=== 'in review'` match |
| FE-M7 | ✅ Fixed rev 2 — `EMPTY_PROFILES` moved below imports |
| FE-M8 | ✅ Fixed rev 2 — stable comment key `${c.author}-${c.createdAt}` |
| FE-L1 | ✅ Fixed rev 2 — "Simphony" → "Symphony" |
| FE-L2 | ✅ Fixed rev 4 — `/* reserved for future log-clear UI */` comment |
| FE-L3 | ✅ Fixed rev 4 — `EMPTY_PROFILE_LABEL = 'Default'`; all 4 sites updated |
| FE-L4 | ✅ Fixed rev 4 — IIFE removed; `isProfileLocked` hoisted |
| FE-L5 | ✅ Fixed rev 2 — "✕ Discarded" feedback text |
| FE-L6 | ✅ Fixed rev 2 — dead `setState` calls removed |
| FE-L7 | ✅ Fixed rev 2 — `handleStateChange` wrapped in `try/catch` |
| FE-L8 | ✅ Fixed rev 4 — ARIA `role="dialog"`, `aria-modal`, `aria-labelledby`; Escape key handler |
| FE-L9 | ✅ Fixed rev 2 — comment added to `optimisticPauseSnapshot` |
| FE-L10 | ✅ Fixed rev 2 — `@deprecated` JSDoc on `symphony.ts` |
| FE-L11–L13 | ✅ Fixed rev 3/4 — typos; stable `key` props |
| FE-NEW-1 | ✅ Fixed rev 5 — `orchestratorState` confirmed in schema; `.optional()` removed |
| FE-NEW-2 | ✅ Fixed rev 5 — 1-second `setTimeout` removed from `useCancelIssue.onSuccess` |
| FE-NEW-3 | ✅ Fixed rev 5 — `useResumeIssue.onMutate` pushes minimal `RunningRow` |
| FE-NEW-4 | ✅ Fixed rev 5 — `RateLimitBar` reads `trackerKind`; shows GitHub/Linear/API label |
| FE-NEW-5 | ✅ Fixed rev 6 — `BoardColumn` outer div gets `max-h-[85vh]`; inner removes `calc(100vh-300px)` |
| FE-NEW-6 | ✅ Fixed rev 5 — settings mutations call `refreshSnapshot()` not `patchSnapshot()` |
| FE-NEW-7 | ✅ Fixed rev 5 — `appendTokenSample` short-circuits on unchanged value |
| FE-NEW-8 | ✅ Fixed rev 5 — `Settings/index.tsx` key uses `JSON.stringify` |
| FE-R5-1 | ✅ Fixed rev 6 — `EMPTY_RUNNING` comment strengthened: explains referential-equality invariant |
| FE-R5-2 | ✅ Fixed rev 5 — `orchestratorState` not `.optional()` in `TrackerIssueSchema` |
| FE-R5-3 | ✅ Fixed rev 5 — `Dashboard/index.tsx` destructures stable `mutateAsync` |
| FE-R5-4 | ✅ Fixed rev 6 — `useSymphonySSE` uses `getState()` inside effect; empty dep array |
| FE-GA-H2 | ✅ Fixed rev 6 — dnd-kit `data.current` casts guarded with optional chain + early return |
| FE-GA-H3 | ✅ Fixed rev 6 — `useLogStream` uses `getState()` inside effect; `appendLog` removed from dep array |
| FE-GA-M8 | ✅ Fixed rev 6 — `TrackerStatesCard` success-banner timer stored in `saveOkTimerRef`; cleared on unmount |
| GO-NEW-24 | ✅ Fixed rev 7 — `goto doneTransition` replaced with `break transitionLoop` labelled break |
| GO-NEW-26 | ✅ Fixed rev 7 — `validateBackend` returns `error`; missing default binary aborts startup via `fmt.Errorf` |
| FE-GA-H5 | ✅ Fixed rev 7 — `useStableRunning` rewritten with `useEffect`-only pattern; no render-phase setState |
| FE-GA-M1 | ✅ Fixed rev 7 — `makeRollbackHandler` calls `useToastStore.getState().addToast()` on error; `Toast` component added to `App.tsx` |
| FE-GA-M3 | ✅ Fixed rev 7 — all 4 mutation `onError` handlers replaced with `makeRollbackHandler(queryClient)` |
| FE-GA-M6 | ✅ Fixed rev 7 — `refreshSnapshot` catch block logs `console.warn` in dev |
| FE-GA-L2 | ✅ Fixed rev 7 — `web/src/utils/timings.ts` created; `SSE_RECONNECT_BASE_MS`, `SSE_RECONNECT_MAX_MS`, `LOG_STABLE_DELAY_MS`, `SAVE_OK_BANNER_MS` imported at all use sites |
| FE-GA-L7 | ✅ Fixed rev 7 — `aria-label="Decrease max workers"` and `aria-label="Increase max workers"` added to StatusStrip buttons |
| FE-GA-L8 | ✅ Fixed rev 7 — `useIssueLogs` returns `{ data, isLoading, isError }` |
| FE-NEW-24 | ✅ Fixed rev 7 — `useLogStream` `addEventListener('log')` wrapped in try-catch; dev warning on error |
| GO-NEW-27 | ✅ Fixed rev 7 — `completionState` retry loop uses `context.WithTimeout(context.Background(), 60s)` for tracker calls; `ctx.Done()` still watched between retries |
| GO-R8-1 | ✅ Fixed rev 8 — discard goroutine uses `context.WithTimeout(context.Background(), 15s)`; no longer derives from the run context that can be cancelled before the goroutine completes |
| GO-R8-4 | ✅ Fixed rev 8 — `readTimeoutMs`/`turnTimeoutMs` snapshotted under `cfgMu.RLock()` at start of `runWorker`; `RunTurn` call uses snapshots |
| GO-R8-6 | ✅ Fixed rev 8 — `runAfterHook` accepts `hook string, timeoutMs int` parameters; callers pass the snapshotted `afterRunHook`/`hookTimeoutMs` instead of reading live config |
| FE-R8-005 | ✅ Fixed rev 8 — render-phase double `setState` in `SessionAccordion` replaced with `useRef` (prevSectionCountRef) + `useEffect`; no more concurrent-React violations |
| FE-R8-007 | ✅ Fixed rev 8 — `toastStore` tracks auto-dismiss timers in `_timers: Map`; `removeToast` cancels the pending timer so manual dismiss and auto-dismiss don't race |
| FE-R8-017 | ✅ Fixed rev 8 — `TOAST_DISMISS_MS = 4_000` added to `utils/timings.ts`; `toastStore` imports it |
| FE-R9-8 | ✅ Fixed rev 9 — `AgentQueueView.onDragStart` unsafe dnd-kit cast guarded with `data?.issue` optional-chain (mirrors Dashboard fix from FE-GA-H2) |
| FE-R9-9 | ✅ Fixed rev 9 — `useSymphonySSE.onerror`: added `reconnectTimer === null` guard so double-error events can't stack duplicate reconnect timers |
| FE-R9-14 | ✅ Fixed rev 9 — SSE catch block now logs `console.warn` in dev for Zod schema validation failures, not just JSON parse errors |
| FE-R9-4 | ✅ Fixed rev 9 — `useTerminateIssue`, `useTriggerAIReview`, `useReanalyzeIssue` all have `onError` toast handlers; previously these mutations failed silently |
| GO-R9-2 | ✅ Fixed rev 9 — `fmt.Errorf("%s", msg)` replaced with `errors.New(msg)` at orchestrator.go:1491 |
| GO-R9-L1 | ✅ Fixed rev 9 — 7 manual `for k, v := range m { dst[k] = v }` copy loops replaced with `maps.Copy()` across orchestrator.go; `"maps"` import added |
| GO-R5-3 | ✅ Fixed rev 10 — `TerminateIssue` routes running-worker case through new `EventTerminateRunning` event; event loop verifies worker still in `state.Running` before setting `userTerminatedIDs` and calling cancel, eliminating the TOCTOU window |
| GO-NEW-21 | ✅ Fixed rev 10 — `FetchPRContext` accepts `baseBranch string`; auto-detects via `git symbolic-ref refs/remotes/origin/HEAD`, falls back to `origin/main`; `agent.base_branch` config field added |
| GO-R10-1 | ✅ Fixed rev 10 — `EventTerminatePaused` handler copies `BacklogStates`/`ActiveStates` with `append([]string{}, ...)` before releasing `cfgMu` |
| GO-R10-2 | ✅ Fixed rev 10 — `defer hookCancel()` inside turn loop replaced with explicit `hookCancel()` call after hook runs |
| GO-R10-3 | ✅ Fixed rev 10 — tracker `CreateComment` guarded with `prCtx == nil`; no duplicate comment on PR-continuation runs |
| GO-R10-4 | ✅ Fixed rev 10 — `runReviewerWorker` snapshots `Command`, `ReadTimeoutMs`, `TurnTimeoutMs`, `SSHHosts` under `cfgMu.RLock()` at start |
| GO-R10-5 | ✅ Fixed rev 10 — `flush()` breaks when `n == 0` to avoid busy-spin on regular file EOF |
| FE-R10-1 | ✅ Fixed rev 10 — `close` wrapped in `useCallback`; added to Escape `useEffect` dep array; `eslint-disable-line` removed |
| FE-R10-2 | ✅ Fixed rev 10 — optimistic resume row uses `prevIssues` to find the real tracker state instead of `'running'` |
| FE-R10-3 | ✅ Fixed rev 10 — log entry key uses `time-event-message.slice(0,24)`, removing array index |
| FE-R10-4 | ✅ Fixed rev 10 — auto-advance condition simplified to `selectedIdx === prev - 1`; redundant `|| prev === 0` branch removed |
| FE-R10-5 | ✅ Fixed rev 10 — `useSettingsActions` adds `addToast` calls on non-ok HTTP and catch paths |
| FE-R10-6 | ✅ Fixed rev 10 — `handleRefresh` calls `refreshSnapshot()` after `invalidateIssues()` |
| FE-R10-7 | ✅ Fixed rev 10 — `AgentQueueView.onDragEnd` uses optional-chain cast `{ issue?: TrackerIssue } | undefined` on `e.active.data.current` |
| GO-R10-6 | ✅ Fixed rev 11 — `handleLogs` filter parses line as JSON and exact-matches `issue_identifier` field; falls back to substring for non-JSON lines |
| FE-R11-1 | ✅ Fixed rev 11 — `useSettingsActions` `toastError` corrected from `addToast({ message, type })` object to `addToast(msg, 'error')` matching the `(message: string, variant?) => void` signature |
| GO-R11-2 | ✅ Already fixed — `defer postRunCancel()` and `defer cancelTrans()` are inside conditional blocks scoped to the post-run / transition phase (not function-level); both contexts derive from `context.Background()` with explicit 60s timeouts, not from `runWorker`'s lifetime |
| GO-R11-8 | ✅ Fixed rev 11 — `io.Copy(io.Discard, resp.Body)` added before `Close()` in label-removal loop; GitHub DELETE label returns 200+JSON, not 204 — prior "false positive" classification was incorrect |
| GO-R11-7 | ✅ Fixed rev 11 follow-up — removed unused `cfg *config.Config` parameter from `AvailableSlots`; function already read only `state.MaxConcurrentAgents`; all 5 call sites + test updated |
| GO-R11-14 | ✅ Fixed rev 11 follow-up — added clarifying comment to `ReconcileStalls` documenting that `StallTimeoutMs` is read without `cfgMu` because no HTTP setter exists; race-free by design |
| GO-R11-26 | ✅ Fixed rev 11 follow-up — the "false positive" masked a real doc/code mismatch: config comment said "Set to 0 to disable" but `positiveIntField` silently replaced 0 with the 1h default; fixed by switching to `intField` and guarding `WithTimeout` in `claude.go` and `codex.go` (`if turnTimeoutMs > 0`) |
| FE-R11-7 | ✅ Fixed rev 11 follow-up — replaced `useMemo`+ref mutation with pure `useMemo` (wantStart/wantEnd) + `useEffect` for viewport accumulation; reset uses React "adjusting state during rendering" pattern with `useState` instead of ref mutation |
| FE-R11-9 | ✅ Fixed rev 11 follow-up — `AgentLogPanel` Zustand selector narrowed from `s.snapshot` (full object, re-renders on every SSE push) to an inline boolean computation; component now only re-renders when `isLive` changes |
| FE-R11-18 | ✅ Already fixed — `Logs/index.tsx` uses `a.click()` directly (modern approach); anchor is not detached; `revokeObjectURL` timing is acceptable; download works cross-browser |
| FE-R11-22 | ✅ Already fixed rev 11 — `Toast.tsx` container has `aria-live="polite"` and `aria-atomic="false"`; verified present in the component |
| FE-R11-2 | ✅ Already fixed — verified rev 13: `useSymphonySSE` uses `getState()` inside effect |
| FE-R11-3 | ✅ Already fixed — verified rev 13: `useLogStream` uses `getState()` inside effect |
| FE-R11-4 | ✅ Already fixed — verified rev 13: `Toast.tsx` + `toastStore` in place |
| FE-R11-5 | ✅ Already fixed — verified rev 13: `TOAST_DISMISS_MS` and other timing constants in `utils/timings.ts` |
| FE-R11-6 | ✅ Already fixed — verified rev 13: `RateLimitBar` `aria-label` present |
| FE-R11-8 | ✅ Already fixed — verified rev 13: `AgentQueueView` and `Dashboard` dnd-kit casts use optional-chain |
| FE-R11-10 | ✅ Already fixed — verified rev 13: `useIssueLogs` returns `isError` |
| FE-R11-12 | ✅ Already fixed — verified rev 13: `useSettingsActions` has toast on error/catch |
| FE-R11-21 | ✅ Already fixed — verified rev 13: `BoardColumn` `max-h-[85vh]` present |
| FE-R11-24 | ✅ Already fixed — verified rev 13: `useLogStream` `addEventListener` wrapped in try-catch |
| FE-R11-25 | ✅ Already fixed — verified rev 13: `useSymphonySSE` Zod schema error logged as `console.warn` |
| FE-R11-32 | ✅ Already fixed — verified rev 13: `handleRefresh` calls `refreshSnapshot()` after `invalidateIssues()` |
| FE-R11-11 | ✅ Fixed rev 13 — log entry keys changed from index-based (`i`) to composite semantic key `time-event-message.slice(0,24)` in both `Logs/index.tsx` and `Timeline/index.tsx` |
| FE-R11-13 | ✅ Fixed rev 13 — dead components (`MetricsCards`, `PausedSessionsTable`, `RetryQueueTable`, `TokenSparkline`) deleted; `apexcharts`/`react-apexcharts` removed from `package.json` and `index.css` |
| FE-R11-16 | ✅ Fixed rev 13 — local `fmtMs` function removed from `Timeline/index.tsx`; now imported from `utils/format` |
| FE-R11-19 | ✅ Fixed rev 13 — `RunningSessionsTable` clickable row `div` gets `role="button"`, `tabIndex={0}`, and `onKeyDown` Enter/Space handler |
| FE-R11-23 | ✅ Fixed rev 13 — `TagInput` component extracted to `web/src/components/symphony/TagInput.tsx`; `TrackerStatesCard` refactored to use it for both active and terminal state chip inputs |
| FE-R11-27 | ✅ Fixed rev 13 — `eslint.config.js` `no-restricted-imports` rule added blocking `**/types/symphony` barrel import |
| FE-R11-29 | ✅ Fixed rev 13 — `KeyboardSensor` added to dnd-kit sensors in both `Dashboard/index.tsx` and `AgentQueueView.tsx` |
| FE-R11-30 | ✅ Fixed rev 13 — terminal palette tokens (`--color-terminal-void`, `--color-terminal-base`, `--color-terminal-header`, `--color-terminal-cursor`) added to `@theme {}` in `index.css`; inline hex `style` attributes in `Logs/index.tsx` replaced with `bg-terminal-*` utility classes |
| GO-R11-1 | ✅ Confirmed fixed prior revision (rev 12 verification) — reconcile functions already operate on snapshotted State fields; no live *config.Config mutable reads in event-loop path |
| GO-R11-3 | ✅ Confirmed fixed prior revision (rev 12 verification) — copyRunningMap deep-copies RunEntry values; WorkerCancel excluded from snapshot |
| GO-R11-4 | ✅ Confirmed fixed prior revision (rev 12 verification) — DispatchReviewer uses reviewerWg; orchestrator waits on shutdown |
| GO-R11-5 | ✅ Confirmed fixed prior revision (rev 12 verification) — auto-clear goroutines tracked via clearWg |
| GO-R11-6 | ✅ Confirmed fixed prior revision (rev 12 verification) — blocking send pattern in place for EventDiscardComplete |
| GO-R11-9 | ✅ Fixed rev 12 — enrichIssue() extracted in cmd/symphony/main.go; both fetchIssues and fetchIssue closures call it |
| GO-R11-10 | ✅ Confirmed fixed prior revision (rev 12 verification) — reconcile.go uses dispatch.go versions; duplicates removed |
| GO-R11-11 | ✅ Fixed rev 12 — generic boundedDo[T] helper extracted in internal/tracker/github/client.go |
| GO-R11-12 | ✅ Fixed rev 12 — BeforeRemove hook called in Manager.RemoveWorkspace in internal/workspace/manager.go |
| GO-R11-13 | ✅ Confirmed fixed prior revision (rev 12 verification) — ReconcileStalls passes entry.RetryAttempt+1 |
| GO-R11-15 | ✅ Fixed rev 12 — goroutine-safety doc comment added to liquidEngine singleton in internal/prompt/renderer.go |
| GO-R11-16 | ✅ Fixed rev 12 — fetchByStatesPage and fetchByIDsPage converted to iterative with maxPaginationPages=200 guard |
| GO-R11-17 | ✅ Confirmed fixed prior revision (rev 12 verification) — expandTilde logs warning and returns unexpanded path on error |
| GO-R11-18 | ✅ Confirmed fixed prior revision (rev 12 verification) — maxWorkersCap, hookFallbackTimeout, postRunTimeout, maxTransitionAttempts defined as constants |
| GO-R11-19 | ✅ Confirmed fixed prior revision (rev 12 verification) — runWorker resides in worker.go after orchestrator split; 885-line count corrected in rev 11 |
| GO-R11-22 | ✅ Fixed rev 12 — RateLimiter optional interface added to internal/tracker/tracker.go; both clients implement RateLimitSnapshot(); main.go uses interface assertion |
| GO-R11-23 | ✅ Fixed rev 12 — maxReviewerAttempts=2 retry loop added to runReviewerWorker in internal/orchestrator/reviewer.go |
| GO-R11-25 | ✅ Confirmed fixed prior revision (rev 12 verification) — issueProfilesMu is sync.RWMutex; Snapshot() uses RLock/RUnlock |
| GO-R11-27 | ✅ Fixed rev 12 — profile Command validated against shell metacharacters in internal/config/validate.go |
| GO-R11-28 | ✅ Fixed rev 14 — ReconcileStalls sends stalledEntry (RunEntry{TerminalReason: TerminalStalled}); handleEvent has TerminalStalled case calling recordHistory; EventReviewerCompleted routes reviewer history through event loop |
| GO-R11-29 | ✅ Confirmed complete rev 14 — orchestrator split into worker.go, reviewer.go, snapshot.go, issue_control.go, event_loop.go, logging.go; orchestrator.go retains only struct, New, Load, constants |
| WIRE-1/11 | ✅ Fixed rev 14 — handleIssue deleted from handlers.go; GET /api/v1/{identifier} route removed from server.go |
| WIRE-3/10 | ✅ Fixed rev 14 — useClearIssueLogs wired to "✕ clear" button in Logs page terminal toolbar; reserved comment removed |
| WIRE-6 | ✅ Fixed rev 14 — onDispatch prop added to IssueCard and BoardColumn; BoardView computes backlogStateSet + handleDispatch; "→ Send to queue" button renders on backlog cards |
| WIRE-8/13 | ✅ Removed rev 14 — AgentMode feature intentionally dropped; setAgentMode dead code removed from useSettingsActions |
| WIRE-12 | ✅ Fixed rev 14 — tuiCancel posts tracker comment matching HTTP cancel path; pauseIssue := tuiCancel for DRY |
| WIRE-14 | ✅ Fixed rev 14 — useIssue(identifier) query added with staleTime: 0; IssueDetailModal fetches fresh data on open with bulk-list fallback |
| WIRE-15 | ✅ Fixed rev 14 — "✦ Review" button added to RunningSessionsTable action bar; calls useTriggerAIReview |
| FE-R11-15 | ✅ Removed rev 14 — AgentMode feature intentionally dropped; not implementing Settings UI selector |
